export class ReportProperties {
    constructor(reportName, reportType, reportWidth, reportHeight)
    {
        this.reportName = reportName;
        this.reportType = reportType;
        this.reportWidth = reportWidth;
        this.reportHeight = reportHeight;
    }
}